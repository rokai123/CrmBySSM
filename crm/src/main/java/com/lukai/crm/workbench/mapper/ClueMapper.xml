<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lukai.crm.workbench.mapper.ClueMapper">
	<!-- 线索的基础结果映射 -->
	 <resultMap id="BaseResultMap" type="com.lukai.crm.workbench.domain.Clue">
	    <id column="id" jdbcType="CHAR" property="id" />
	    <result column="fullname" jdbcType="VARCHAR" property="fullname" />
	    <result column="appellation" jdbcType="VARCHAR" property="appellation" />
      <result column="owner" jdbcType="CHAR" property="owner" />
      <result column="company" jdbcType="VARCHAR" property="company" />
      <result column="job" jdbcType="VARCHAR" property="job" />
      <result column="email" jdbcType="VARCHAR" property="email" />
      <result column="phone" jdbcType="VARCHAR" property="phone" />
      <result column="website" jdbcType="VARCHAR" property="website" />
      <result column="mphone" jdbcType="VARCHAR" property="mphone" />
      <result column="state" jdbcType="VARCHAR" property="state" />
      <result column="source" jdbcType="VARCHAR" property="source" />
      <result column="create_by" jdbcType="CHAR" property="createBy" />
      <result column="create_time" jdbcType="CHAR" property="createTime" />
      <result column="edit_by" jdbcType="CHAR" property="editBy" />
      <result column="edit_time" jdbcType="CHAR" property="editTime" />
      <result column="description" jdbcType="VARCHAR" property="description" />
      <result column="contact_summary" jdbcType="VARCHAR" property="contactSummary" />
      <result column="next_contact_time" jdbcType="VARCHAR" property="nextContactTime" />
      <result column="address" jdbcType="VARCHAR" property="address" />
  	</resultMap>
	<sql id="Base_Column_List">
	    id, fullname, appellation, owner, company, job, email, phone, website, mphone, state, source, create_by, create_time, edit_by, edit_time, description, contact_summary,next_contact_time, address
	</sql>
	
	<!-- 根据条件分页查询线索信息 -->
	<select id="selectClueByConditionForPage" parameterType="map" resultMap="BaseResultMap">
        SELECT
          c.id, c.fullname, c.appellation, u1.name as owner, c.company, c.job, c.email, c.phone, c.website,
          c.mphone, c.state, c.source, u2.name as create_by, c.create_time, u3.name as edit_by, c.edit_time, c.description, c.contact_summary,c.next_contact_time, c.address
        FROM tbl_clue c join tbl_user u1 on c.owner = u1.id 
                        join tbl_user u2 on c.create_by = u2.id
                        left join tbl_user u3 on c.edit_by = u3.id
		<where>
		    <if test="fullname != null and fullname != ''">
		        fullname like '%' || #{fullname} || '%'
		    </if>
		    <if test="company != null and company != ''">
		        and company like '%' || #{company} || '%'
		    </if>
		    <if test="phone != null and phone != ''">
		        and phone like '%' || #{phone} || '%'
		    </if>
		    <if test="source != null and source != ''">
		        and source like '%' || #{source} || '%'
		    </if>
		    <if test="owner != null and owner != ''">
		        and owner like '%' || #{owner} || '%'
		    </if>
		    <if test="mphone != null and mphone != ''">
		        and mphone like '%' || #{mphone} || '%'
		    </if>
		    <if test="state != null and state != ''">
		        and state like '%' || #{state} || '%'
		    </if>
		</where>
<!--        order by c.create_time desc-->
<!--        limit #{beginNo},#{pageSize}-->
    </select>

</mapper>